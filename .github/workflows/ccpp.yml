name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Build DataDisplay
      run: |
        cd “C:\Program Files (x86)\Microsoft Visual Studio\2017\Enterprise\MSBuild\15.0\Bin\” .\MSBuild.exe $Env:GITHUB_WORKSPACE\Projects\datadisplay.vcxproj
